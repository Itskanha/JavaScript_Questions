// 8. **Clear all session storage items:**  

// Clear all items from session storage
sessionStorage.clear();

// Log the number of items in session storage (should be 0)
console.log(sessionStorage.length); // Output: 0